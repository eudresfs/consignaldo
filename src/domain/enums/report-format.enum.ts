/**
 * Formatos de exportação suportados para relatórios
 */
export enum ReportFormat {
  PDF = 'PDF',           // Formato PDF para visualização
  EXCEL = 'EXCEL',       // Planilha Excel para análise
  CSV = 'CSV',           // CSV para importação em outros sistemas
  HTML = 'HTML',         // HTML para visualização web
  JSON = 'JSON',         // JSON para integração via API
}
